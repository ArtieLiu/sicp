; The two inputs of and gate cannot be set at the same time, 
; they must be set one by one. 
; The actions saved in queue should be executed by the order they occur and this is gaureented by using FIFO queue. 
; If a LIFO queue is used, the actions in the siumulation would be executed in reverse time.
